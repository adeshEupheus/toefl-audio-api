import { RequestHandler } from "express";
export declare const validateSchool: RequestHandler;
export declare const validateTest: RequestHandler;
export declare const validateAudio: RequestHandler;
export declare const validateUser: RequestHandler;
